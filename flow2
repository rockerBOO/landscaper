List

key/value



landscaper-usda-list

landscaper-processed-usda-list

landscaper



Injest
- Use or generate list to process

plantList = List.FromCSV('./data/plantlist')

for info in plantList:
	# import plant

	key = data.save(Plant)

	if key == False:
		print('Failed to save plant')
		continue

	# add plant to list

	processor.add(key)

Process

# Lock row
plantList = cur.select("select * from list where job_id = 1")

work = processor.getWork()
	
plant = Plant(info)

data.get(info['key'])

plant.Properties = translate(properties, propertyMap)

data.save('processed-plant-key', plant)

processor.finish()

Organize

for variable in variables:
	
	#matching variable
	variable.Height = 5

	#range variable
	variable.Height = (5, 6)

	variable.Height/variable.BloomTime/variable.BloomLength

	#function variable
	variable_function = lamba function (variable): variable*2

Present/Assemble